id=${moduleId}
name=${moduleName}
version=${versionName}
versionCode=${versionCode}
author=JingMatrix, maintained by igorcv88
description=Experimental PostBoot build with DEFEX-safe /dev tmpfs staging and idempotent monitor bootstrap.
updateJson=${updateJson}
