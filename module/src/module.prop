id=${moduleId}
name=${moduleName}
version=${versionName}
versionCode=${versionCode}
author=JingMatrix, maintained by igorcv88
description=Experimental PostBoot build with DEFEX-safe /dev runtime, idempotent monitor bootstrap, and one-shot verified zygote activation without a global soft reboot.
updateJson=${updateJson}