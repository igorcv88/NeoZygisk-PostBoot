id=${moduleId}
name=${moduleName}
version=${versionName}
versionCode=${versionCode}
author=JingMatrix, maintained by igorcv88
description=Hardware-validated PostBoot build with a DEFEX-safe /dev runtime, idempotent monitor bootstrap, and restart-free verification after an external KernelSU Manager Soft Reboot.
updateJson=${updateJson}
